@page
@model JewlryShop2.Pages.Carts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Carts</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
          @*    שינוי לנראות טובה יותר  *@
           @*  <th>
                @Html.DisplayNameFor(model => model.Cart[0].CustomerName)
            </th> *@
            <th>
                Customer Name
            </th>
           @*  <th>
                @Html.DisplayNameFor(model => model.Cart[0].CartId)
            </th> *@
            <th>
                Cart Number
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Cart) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Name)
            </td>
            <td>
                 @Html.DisplayFor(modelItem => item.CartId)
             </td>
            <td>
                <a class="btn btn-outline-secondary" asp-page="/Items/CartItems.lst" asp-route-CartId="@item.CartId">Items in Cart</a>
                <a asp-page="./Edit" asp-route-id="@item.CartId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CartId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CartId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
